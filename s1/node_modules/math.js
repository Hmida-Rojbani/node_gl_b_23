module.exports = {
  sum: (tab) => {
    var sum = 0;
    tab.forEach((element) => (sum += element));
    return sum;
  },
  mul: (tab) => {
    var mul = 1;
    tab.forEach((element) => (mul *= element));
    return mul;
  },
};
